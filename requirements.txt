# Direct dependencies for the RL-guided simplex solver.
# Pinned to the versions the thesis results were produced with.
#
# This is the curated, human-readable dependency list. For the exact frozen
# environment (all transitive deps, CUDA wheels, etc.) install from
# requirements-lock.txt instead:
#     pip install -r requirements-lock.txt

numpy==2.2.3
scipy==1.15.2
torch==2.6.0
stable_baselines3==2.5.0
gymnasium==1.0.0
pandas==2.2.3
matplotlib==3.10.0
tensorboard==2.19.0
tqdm==4.67.1
rich==13.9.4

# Leduc-poker experiments only (GAME_MODE = "leduc"). OpenSpiel is not on PyPI
# as a simple wheel for all platforms and is often built from source, so it is
# intentionally left uncommented-out here — install it separately, e.g.:
#     pip install open_spiel
# and confirm with:  python -c "import pyspiel"
# (The matrix / normal-form experiments do NOT need it.)
